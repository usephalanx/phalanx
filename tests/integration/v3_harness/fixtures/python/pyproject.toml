[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "harness-python-fixture"
requires-python = ">=3.11"

[project.optional-dependencies]
tests = ["pytest", "pytest-cov"]

[tool.ruff]
lint.select = ["E", "F"]
lint.future-annotations = true
