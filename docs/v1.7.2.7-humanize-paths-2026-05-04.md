# v1.7.2.7 — humanize Path 1 + Path 2 (2026-05-04)

**Stack**: v1.7.2.7 (commits `9684013` → `1a1d845`, deployed 19:00 UTC)
**Window**: 19:14 → 20:00 UTC (~46 min total)
**Result**: ✅ **All 5 hard pass criteria met. Both paths SHIPPED.**

This is the second external-OSS proof point on v1.7.2.7. Path 1 was the
2026-05-03 DoD baseline; Path 2 is a NEW commit (different file, different
maintainer fix shape) added today. Both ran post-deploy of the v1.7.2.5
→ v1.7.2.7 batch.

---

## 1. Hard pass verdict

| Criterion | Threshold | Actual | Status |
|---|---|---|---|
| SHIP rate | ≥ 1/2 | **2/2 (100%)** | ✅ PASS |
| False ships | 0 | **0** | ✅ PASS |
| Unsafe ships | 0 | **0** | ✅ PASS |
| SHA mismatches | 0 | **0** | ✅ PASS |
| Malformed apply_diff reaching engineer | 0 | **0** (apply_diff used 0 times) | ✅ PASS |

---

## 2. Path 1 — humanize tz-aware datetime fix re-derivation ✅

**Reverted maintainer commit**: `a47a89e` ("fix: handle tz-aware datetimes in naturalday and naturaldate", PR #297)
**Reverted file**: `src/humanize/time.py` (kept the tests in place)

| Field | Value |
|---|---|
| run_id | `a820d5a4-6b08-4f6c-8da2-188c59bdc0e1` |
| Repo / PR | usephalanx/humanize#15 |
| Verdict | **SHIPPED** |
| Iterations | 1 |
| Total time | ~20 min (5 min agent + 225s gate poll + setup/teardown) |
| TL strategy | sre_setup → engineer → sre_verify |
| TL root_cause | *"The PR regressed timezone-aware date comparison by replacing tz-local 'today' with `dt.date.today()`, so `naturaldate()` misclassifies aware datetimes..."* |
| TL confidence | 0.99 |
| TL verify_command | `pytest tests/test_time.py::test_naturaldate_tz_aware -q` |
| Patch actions | **read, replace, replace, commit, push, run** |
| `apply_diff` used? | **No** |
| `replan_reason` | n/a (single iter) |
| Engineer commit | `0a314779b653337e2d543a1d8c3df3f66dc3a1c2` |
| Verified commit | `0a314779b653337e2d543a1d8c3df3f66dc3a1c2` |
| `sha_match` | ✅ True |
| Sandbox sync method | `git_fetch_reset_hard` (ok=true) |
| Failure fingerprint | `ad88aefb8227524a` |
| Challenger verdict | accept |
| **Gate decision** | **TRUE_GREEN** (poll_seconds = 225s) |
| Fixed checks | **30** (Python 3.10 / 3.11 / 3.12 / 3.13 / 3.13t / 3.14 / 3.14t / 3.15 / 3.15t / pypy3.11 × {ubuntu, macos, windows}) |
| Regressed | 0 |
| Still failing | 0 |
| GitHub final state | 26 success + 4 skipped (release / publish jobs — irrelevant to PR) |
| False ship? | **No** (every fixed check truly green) |
| Regression? | **None** |
| Estimated cost | ~$3.00 |

**Engineer's diff** (functionally equivalent to the maintainer's `a47a89e`):

```diff
+    if isinstance(value, dt.datetime) and value.tzinfo is not None:
+        today = dt.datetime.now(value.tzinfo).date()
+    else:
+        today = dt.date.today()
-    delta = value - dt.date.today()
+    delta = value - today
```

(applied to BOTH `naturalday` and `naturaldate`; `naturaldate` also captures
`original_value` to preserve tz info through nested `naturalday` call.)

---

## 3. Path 2 — humanize intword decimal-separator locale fix re-derivation ✅

**Reverted maintainer commit**: `7175184` ("Add locale support for decimal separator in intword", PR #287, merged via `e87f2e2`)
**Reverted file**: `src/humanize/number.py`
**Trigger script**: NEW — `scripts/v3_path2_humanize_intword.sh` (today)

| Field | Value |
|---|---|
| run_id | `7b75e396-8c25-4f4b-a639-f6fe52c8a7b5` |
| Repo / PR | usephalanx/humanize#16 |
| Verdict | **SHIPPED** |
| Iterations | 1 |
| Total time | ~21 min (5 min agent + 210s gate poll + setup/teardown) |
| TL strategy | sre_setup → engineer → sre_verify |
| TL root_cause | *"tests/test_i18n.py::test_intword_i18n fails because src/humanize/number.py:intword returns '1.0'/'1.2' strings instead of locale-decimal '1,0'/'1,2'..."* |
| TL confidence | 0.99 |
| TL verify_command | `python -m pytest tests/test_i18n.py::test_intword_i18n -q` |
| Patch actions | **read, replace, commit, push, run** |
| `apply_diff` used? | **No** |
| `replan_reason` | n/a (single iter) |
| Engineer commit | `4de8c30e054b398744e5be10afb54bef336b6460` |
| Verified commit | `4de8c30e054b398744e5be10afb54bef336b6460` |
| `sha_match` | ✅ True |
| Sandbox sync method | `git_fetch_reset_hard` (ok=true) |
| Failure fingerprint | `d58208d5b1e2a148` |
| Challenger verdict | accept |
| **Gate decision** | **TRUE_GREEN** (poll_seconds = 210s) |
| Fixed checks (in gate poll window) | 20 (a subset that had reported by 210s) |
| GitHub final state (post-soak) | **30** check-runs: 28 success + 2 skipped (all matrix combos × 3 OS green at full settle) |
| Regressed | 0 |
| Still failing | 0 |
| False ship? | **No** |
| Regression? | **None** |
| Estimated cost | ~$3.00 |

**Engineer's diff** (functionally equivalent to maintainer's `7175184`):

```diff
@@ -261,7 +261,9 @@ def intword(value: NumberOrString, format: str = "%.1f") -> str:

     singular, plural = human_powers[ordinal]
     unit = _ngettext(singular, plural, math.ceil(rounded_value))
-    return f"{negative_prefix}{format % rounded_value} {unit}"
+    decimal_sep = decimal_separator()
+    number = (format % rounded_value).replace(".", decimal_sep)
+    return f"{negative_prefix}{number} {unit}"
```

The fix calls into `decimal_separator()` (an existing helper in
`src/humanize/i18n.py`) to swap `.` for the locale's decimal mark
(e.g. `,` in fr_FR / de_DE). Identical effect to the maintainer's
original PR.

---

## 4. Aggregate metrics — both paths

| | Path 1 | Path 2 | Total |
|---|---|---|---|
| Verdict | SHIPPED | SHIPPED | 2/2 |
| Iterations | 1 | 1 | — |
| Wall-clock | ~20 min | ~21 min | ~41 min |
| Agent time | ~5 min | ~5 min | — |
| Gate poll | 225s | 210s | — |
| Patch actions | read+replace+replace | read+replace | — |
| `apply_diff` used | 0 | 0 | **0** |
| Patch safety triggered | 0 | 0 | **0** |
| GitHub matrix size | 30 | 30 | — |
| All matrix green at settle | ✅ | ✅ | — |
| Estimated cost | ~$3.00 | ~$3.00 | ~$6.00 |

---

## 5. v1.7.2.7 guard activity on these runs

| Guard | Path 1 | Path 2 |
|---|---|---|
| Plan validator: fuzzy `apply_diff` | not exercised (no apply_diff) | not exercised |
| Plan validator: ≤5-hunk threshold | not exercised | not exercised |
| Plan validator: completeness | passed | passed |
| Plan validator: replan_reason | not exercised (single iter) | not exercised |
| Plan validator: same-strategy | not exercised | not exercised |
| c8 self-critique (flake) | not exercised (not a flake) | not exercised |
| Patch safety R3-R7 | not exercised | not exercised |
| Sandbox exact-sha sync | ✅ git_fetch_reset_hard ok | ✅ git_fetch_reset_hard ok |
| GitHub check-gate | ✅ TRUE_GREEN, 30 fixed | ✅ TRUE_GREEN, 30 (full settle) |
| Commander REPLAN priors | not exercised | not exercised |

**Both runs were single-iter — clean diagnosis, clean engineer step,
clean verify, clean gate.** None of the new validators had to fire,
because TL emitted a clean plan on the first try. The architecture
underneath (sha sync, narrow verify, GitHub gate) carried the run.

---

## 6. Comparison vs prior humanize attempts

| Metric | 2026-05-03 DoD (Path 1, v1.7.2.4) | 2026-05-04 today (v1.7.2.7) |
|---|---|---|
| Path 1 verdict | SHIPPED, 1 iter, TRUE_GREEN | **SHIPPED, 1 iter, TRUE_GREEN** |
| Path 1 actions | read + replace (2 hunks via apply_diff conceptually but rendered as replace+replace by today's TL) | **read + replace + replace** |
| Path 1 gate-poll | 180s | 225s (slightly slower CI) |
| Path 2 attempted | No (deferred) | **Yes — also SHIPPED** |

Path 1 result is reproducible across 24h gap. Path 2 is a new datapoint
showing the architecture handles a different fix shape (locale-aware
formatting in number.py, not datetime in time.py).

---

## 7. Anti-pattern / failure-mode summary

| Pattern | Hits |
|---|---|
| Coverage uses apply_diff for small edits | 0 (n/a — these aren't coverage) |
| Flake attempts test deletion | 0 (n/a — not flake) |
| iter-2 same-strategy repeat | 0 (n/a — single iter both runs) |
| Gate NOT_FIXED or REGRESSION | **0** |
| Run exits without terminal state | **0** |
| Unsafe ship (skip directive / test deletion / CI config edit) | **0** |
| sha_match=false on SHIP | **0** |
| `apply_diff` reaches engineer with malformed body | **0** |

---

## 8. The shape that didn't fire — and what that tells us

v1.7.2.5 → v1.7.2.7 added 11 new validators / guards across 3 layers
(plan validator, c8 self-critique, patch safety R6+R7). On these two
humanize runs, **none of them fired** — the path stayed on the green
"single-iter clean ship" route.

That's a feature, not a bug:

- **Plan validator silent** = TL emitted well-formed plans first try.
  Fuzzy diffs, sub-threshold apply_diff, empty plans, missing
  affected_files — all absent. The prompt nudge worked.

- **c8 silent** = TL didn't try test deletion. Both bugs were genuinely
  source-code regressions; TL targeted source fixes correctly.

- **Patch safety silent** = engineer's steps stayed inside affected
  files, didn't touch CI config, didn't drop assertions or remove tests.

- **Replan-strategy silent** = no iter-2 needed.

- **GitHub gate fired (TRUE_GREEN)** = the actual ship signal. 30 matrix
  jobs polled, all green, no regressions detected.

The system is in its **clean fast-path** for both paths, which is the
production goal.

---

## 9. New artifacts checked in

- `scripts/v3_path2_humanize_intword.sh` — Path 2 trigger (parameterized
  copy of Path 1's tz-revert script, targeting commit `7175184` on
  `src/humanize/number.py`)
- `docs/v1.7.2.7-humanize-paths-2026-05-04.md` — this report

---

## 10. Verdict

**5/5 hard pass criteria met.** Both humanize paths SHIPPED on v1.7.2.7
with full GitHub-matrix green confirmation. No false ships, no
regressions, no unsafe behavior, no malformed `apply_diff` reaching
the engineer.

The v1.7.2.7 stack is now demonstrated against:
- Internal testbed (mini-soak: 5/6 SHIPPED, 1 validator-caught miss)
- External humanize Path 1 (datetime/timezone)
- External humanize Path 2 (intword locale decimal)

Recommended next step: longer 24h soak OR a third real-world repo to
build a broader proof-point distribution before marketplace.
