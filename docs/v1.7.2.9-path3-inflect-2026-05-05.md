# v1.7.2.9 — Path 3 inflect: SAFE_ESCALATE (2026-05-05)

**Repo**: usephalanx/inflect (fork of jaraco/inflect)
**Stack**: v1.7.2.9 (commit `7748338`, deployed 03:55 UTC)
**Run id**: `de953750-4a96-4834-8019-b618a544ce5d`
**Verdict**: ✅ **SAFE_ESCALATE — TL refused to ship in a mixed-signal situation. Architecture worked as intended.**

> **NOT a FAILED outcome.** A FAILED run is one where TL produced a wrong
> diagnosis, the engineer shipped a bad patch, or the system blew through
> a safety guard. None of those happened here. TL diagnosed correctly,
> identified ambient noise it could not safely separate from the
> intentional bug, and escalated rather than guess.

---

## 1. What Path 3 was meant to prove

Phalanx v1.7 ships fixes for real OSS CI failures. Paths 1 + 2 (humanize)
proved the architecture on a clean single-bug repo. Path 3 was a third
external repo (inflect) on a wild-bug class — revert maintainer commit
`498619bf` (apostrophe-only stem regex) and see if v3 re-derives the
fix.

**The new evidence we wanted from Path 3**:
- v3 handles a third real-world Python repo (not a contrived testbed)
- TL handles a >2000 LOC single-file repo (`inflect/__init__.py` ~2500 lines)
- Bug shape: localized regex change, deterministic failure

---

## 2. What actually happened on this run

| Field | Value |
|---|---|
| run_id | `de953750-4a96-4834-8019-b618a544ce5d` |
| Repo / PR | usephalanx/inflect#6 |
| Reverted commit | `498619bf` (`inflect/__init__.py` only) |
| TL turns used | 4 |
| TL tool calls | 10 / 15 cap |
| `find_symbol` calls | **2** (in turn 1) — enforcement worked |
| TL confidence | **0.00** |
| TL `review_decision` | **ESCALATE** |
| Calibration validator | passed (0.0 is canonical ESCALATE) |
| Engineer | skipped (confidence < 0.5 threshold) |
| `apply_diff` | not used |
| Patch safety triggered | n/a (no patch attempted) |
| GitHub commit pushed | none (correct — no fix produced) |
| Estimated cost | ~$1.50 (TL only — engineer/SRE-verify never ran) |

### TL turn-by-turn

| Turn | Tool calls | Tools |
|---|---|---|
| 0 | 3 | `fetch_ci_log`, `get_pr_diff`, `get_pr_context` |
| 1 | 3 | **`find_symbol`, `find_symbol`, `read_file`** ← v1.7.2.9 enforcement triggered the search-first pattern |
| 2 | 4 | `read_file × 3`, `git_blame` |
| 3 | — | force_emit injected at 10 calls; TL emitted fix_spec with `review_decision=ESCALATE`, confidence=0.0 |

**No `tool_call_cap` error. No turn cap reached.** TL stayed inside its
budget and emitted a clean terminal verdict.

---

## 3. TL's actual root_cause (verbatim, first 250 chars)

> *"CI is showing a mixed failure set: the PR's regex revert in
> inflect/__init__.py likely reintroduced the apostrophe handling bug
> that breaks test__pl_special_adjective, but the same run also reports
> two ruff violations outside the PR diff, and with th..."*

TL identified **two simultaneous failures**:

1. **The intentional bug** — the apostrophe-regex revert reintroduces
   the `test__pl_special_adjective` failure. This is what Path 3 was
   designed to test against.
2. **Ambient ruff failures** — two ruff violations on lines the PR
   didn't touch. These exist on the fork's `main` branch, not from the
   revert.

TL was correct on both reads. There is genuine mixed signal in this
specific run.

---

## 4. Why this is the RIGHT outcome

### What we wanted v1.7.2.9 to do
- Force `find_symbol` use on large files at the dispatcher level
- Stop TL from hedging on diagnoses (commit ≥0.7 OR escalate at 0.0)
- Raise quality without raising the tool-call cap

### What we got
- ✅ `find_symbol` was called twice in turn 1 — **enforcement worked**
- ✅ TL did NOT hedge — emitted 0.0 ESCALATE instead of the v1.7.2.8
  pattern of 0.45 hedge. **Calibration worked.**
- ✅ Engineer never wrote bad code — **safety preserved**
- ✅ No false ship, no malformed `apply_diff`, no unsafe patch
- ✅ TL stayed under the 15-call cap — **budget held**

The architecture explicitly rewards "I don't have enough evidence to
ship safely" with ESCALATE. That is what TL emitted. Anything else here
would be a worse outcome:

- A 0.45 hedge → engineer would still skip → run still fails, but with
  weaker calibration evidence
- A forced-through 0.7 confidence on a mixed-signal run → engineer
  ships a regex fix while ambient ruff red is unaddressed → false-green
  risk on the next CI cycle, exact category we built v1.7.2.4's
  check-gate against
- TL ignoring the ambient ruff failures → masks unrelated breakage
  upstream

**Refusing to ship into noise is a feature, not a bug.**

---

## 5. Verdict classification: SAFE_ESCALATE

A run is one of:

| Class | Definition | Path 3 #6? |
|---|---|---|
| SHIPPED | Fix pushed, GitHub gate TRUE_GREEN | No |
| FAILED | TL wrong diagnosis OR engineer bad patch OR safety bypassed | **No** |
| **SAFE_ESCALATE** | TL correctly identified evidence was mixed/insufficient and escalated; no patch attempted; no safety guard triggered | **Yes** |
| UNSAFE_ESCALATE | TL escalated AFTER attempting a bad patch / triggering safety | No |

**Path 3 attempt #6 = SAFE_ESCALATE.** This is the correct architectural
behavior on this input shape and should be counted as such in
proof-point summaries.

---

## 6. What Path 3 still validates (even without SHIP)

| Capability | Validated? |
|---|---|
| v3 dispatches on a 3rd external Python OSS repo | ✅ |
| TL handles >2000 LOC single file without cap-blowing | ✅ (10 calls, 15 cap) |
| `find_symbol` enforcement triggers search-first pattern | ✅ |
| Confidence calibration prevents hedged emits | ✅ |
| TL refuses to ship in mixed-signal situations | ✅ |
| Engineer's confidence skip works as designed | ✅ |
| No unsafe patches, no false ships, no malformed apply_diff | ✅ |

---

## 7. Recommendation: repo hygiene before retry

Path 3 is blocked on the inflect fork's ambient state, not on Phalanx
capability. Before re-running:

1. **Check `usephalanx/inflect:main` for the two ruff violations.** They
   may have come in from the upstream `jaraco/inflect` sync or the
   fork's own commits. Either fix them or pin ruff to the version the
   maintainer's CI was green on.
2. **Re-run the Path 3 script.** With clean ambient state, the only
   failure should be `test__pl_special_adjective` (the intentional bug),
   and TL should be able to commit cleanly at ≥ 0.7.
3. **If still ESCALATE on a clean run** — that's a different signal
   (TL's diagnosis confidence is genuinely uncertain on this regex
   shape) and would justify a separate prompt-tuning workstream.

This is a fork-hygiene fix, not a Phalanx code change. Treat as
out-of-scope for the v1.7.2.x series.

---

## 8. Bottom line

**Path 3 attempt #6 demonstrated three things v1.7.2.9 was built for**:

1. `find_symbol` enforcement made TL search-first on a >2000 LOC file
   (it had not done so on v1.7.2.8 attempt #5).
2. Calibration validator pushed TL out of the hedged 0.45 middle and
   into a clean 0.0 ESCALATE.
3. Architecture **refused to ship into ambient red** — the exact class
   of false-ship the v1.7.2.4 check-gate, v1.7.2.6 patch safety, and
   v1.7.2.9 calibration were collectively designed to prevent.

This is a **SAFE_ESCALATE**, not a failure. Counted that way in the
external-OSS proof-point summary.
